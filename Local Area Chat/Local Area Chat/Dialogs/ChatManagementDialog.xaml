<Window x:Class="Local_Area_Chat.Dialogs.ChatManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat verwalten" Height="450" Width="600" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chat Name und Status -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Chat-Name ändern:" Margin="0,0,0,5"/>
            <TextBox x:Name="ChatNameTextBox" Grid.Row="1" Grid.Column="0" Height="25"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Status:" Margin="0,0,0,5"/>
            <ComboBox x:Name="StatusComboBox" Grid.Row="1" Grid.Column="2" Height="25">
                <ComboBoxItem Content="Öffentlich"/>
                <ComboBoxItem Content="Privat"/>
            </ComboBox>
        </Grid>

        <!-- Chat-Teilnehmer und Benutzer hinzufügen -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Chatteilnehmer -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Chatteilnehmer:" Margin="0,0,0,5"/>
            <ListBox x:Name="ParticipantsListBox" Grid.Row="1" Grid.Column="0" Height="150"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
            <Button x:Name="RemoveUserButton" Grid.Row="2" Grid.Column="0" 
                    Content="Entfernen" Width="80" HorizontalAlignment="Left" Margin="0,5,0,0"
                    IsEnabled="False" Click="RemoveUserButton_Click"/>

            <!-- Benutzer hinzufügen -->
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Benutzer hinzufügen:" Margin="0,0,0,5"/>
            <ListBox x:Name="AvailableUsersListBox" Grid.Row="1" Grid.Column="2" Height="150"
                     DisplayMemberPath="UserName" SelectionChanged="AvailableUsersListBox_SelectionChanged"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5,3"/>
                        <Setter Property="Margin" Value="1"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2196F3"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            <Button x:Name="AddUserButton" Grid.Row="2" Grid.Column="2" 
                    Content="Hinzufügen" Width="80" HorizontalAlignment="Left" Margin="0,5,0,0"
                    IsEnabled="False" Click="AddUserButton_Click"/>
        </Grid>

        <!-- Chatadmin anzeigen -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <TextBlock x:Name="ChatAdminTextBlock" Text="Chatadmin: - Christoph" 
                       FontWeight="Bold" Foreground="#2E7D32"/>
        </Grid>

        <!-- Chat löschen Bereich -->
        <Border Grid.Row="3" Background="#FFEBEE" BorderBrush="#F44336" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Gefährlicher Bereich" FontWeight="Bold" Foreground="#D32F2F" Margin="0,0,0,5"/>
                    <TextBlock Text="Das Löschen des Chats kann nicht rückgängig gemacht werden." 
                               Foreground="#666" TextWrapping="Wrap"/>
                </StackPanel>
                
                <Button x:Name="DeleteChatButton" Grid.Column="1" Content="Chat löschen" 
                        Background="#F44336" Foreground="White" Padding="15,8"
                        Click="DeleteChatButton_Click"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="75" Height="25" Margin="0,0,10,0"
                    IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="Abbrechen" Width="75" Height="25"
                    IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>